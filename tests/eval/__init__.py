"""Phase F evaluation harness package (BEAM-pattern eval over the brain).

Importable both under pytest (``tests`` is a package) and, when the
``hermes brain eval`` CLI verb is wired, under the Hermes loader — the
harness registers the repo root as package ``brain`` on demand.
"""
